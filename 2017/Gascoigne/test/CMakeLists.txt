LINK_LIBRARIES(${GASCOIGNE_LIBS})

ADD_EXECUTABLE("Test1${BUILD_ENDING}" main1.cc)
#ADD_EXECUTABLE("Test2${BUILD_ENDING}" main2.cc)

SET(_SRC ${GASCOIGNESTD_SOURCE_DIR}/test)
ADD_CUSTOM_TARGET(COPY ALL cp ${_SRC}/*.param ${_SRC}/*.inp ${_SRC}/*.gup .)
