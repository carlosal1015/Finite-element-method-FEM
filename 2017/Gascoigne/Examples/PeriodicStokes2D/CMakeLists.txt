
INCLUDE($ENV{HOME}/.gascoignerc)
INCLUDE(${GASCOIGNE_STD}/CMakeGlobal.txt)

LINK_LIBRARIES(${GASCOIGNE_LIBS})

AUX_SOURCE_DIRECTORY(.  SRCFILES)
SET(_SRC ${SRCFILES})

ADD_EXECUTABLE("PeriodicStokes2D" ${_SRC} )


